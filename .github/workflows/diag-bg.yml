name: Diag bg check
on: workflow_dispatch
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: verify with versioned url
        run: |
          echo "--- section-fields rule (versioned, bypass CDN cache) ---"
          curl -s --max-time 20 "https://teima.space/ensaios/assets/css/style.css?v=60102f1727cbe9367587207943c3d6a3c83f59b1" | grep -A2 '\.section-fields {'
