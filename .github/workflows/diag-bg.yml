name: Diag bg check
on: workflow_dispatch
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: curl bg.jpg
        run: |
          echo "--- root ---"
          curl -sD - -o /dev/null https://teima.space/
          echo "--- bg.jpg ---"
          curl -sD - -o /dev/null https://teima.space/bg.jpg
