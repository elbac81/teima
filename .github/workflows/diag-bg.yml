name: Diag bg check
on: workflow_dispatch
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: sample live data
        run: |
          curl -s https://teima.space/ensaios/api.php | head -c 3000
